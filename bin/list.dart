void main() {
//list bisa di isi data duplikat cth:esa,esa
  List<int> listInt = []; //create list of int

  var listString = <String>[];

  print(listInt);
  print(listString);

  var names = <String>['Esa', 'Surya', 'Anggraini'];
  //names.add('Esa');
  //names.add('Surya');
  //names.add('Anggraini');
  print(names);
  print(names.length);

//list.add(value) untuk menambah data ke list
//list[index] untuk mengambil data di list
//list[index] = value untuk mengubah data di list
//list.removeAt(index) untuk menghapus data  di list,index secara otomatis bergeser
}
